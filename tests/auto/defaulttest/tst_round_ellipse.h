#include <gtest/gtest.h>
#include <gmock/gmock-matchers.h>

#include "../../../src/round.h"
#include "../../../src/ellipse.h"

using namespace testing;

/*
TEST(Round_Ellipse_TestSet, )
{
    Round round();
    Ellipse ellipse();

    int res1 = ellipse.intersect(round);
    int res2 = round.intersect(ellipse);

    EXPECT_EQ(res1, res2);
    EXPECT_EQ(res1, );
}
*/
