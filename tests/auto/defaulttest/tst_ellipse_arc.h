#include <gtest/gtest.h>
#include <gmock/gmock-matchers.h>

#include "../../../src/ellipse.h"
#include "../../../src/arc.h"

using namespace testing;

/*
TEST(Ellipse_Arc_TestSet, )
{
    Round ellipse();
    Round arc();

    int res1 = arc.intersect(ellipse);
    int res2 = ellipse.intersect(arc);

    EXPECT_EQ(res1, res2);
    EXPECT_EQ(res1, );
}
*/
